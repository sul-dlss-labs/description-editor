<turbo-stream action="append" target="titlesFrame">
  <template>
    <%= render(TitleComponents::ShowComponent.new(title_model: @title_model, description_id: params[:description_id], index: @index)) %>
  </template>
</turbo-frame>
<turbo-stream action="replace" target="addTitleFrame">
  <template>
    <%= render(TitleComponents::AddComponent.new(description_id: params[:description_id])) %>
  </template>
</turbo-stream>
<turbo-stream action="replace" target="titleCocinaJson">
  <template>
    <%= render(CocinaJsonComponent.new(description_field: :title, props: @description.title)) %>
  </template>
</turbo-stream>
