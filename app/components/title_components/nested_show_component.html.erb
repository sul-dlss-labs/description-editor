<% if title.type.present? %>
  <dl class="row mb-0">
    <dt class="col-sm-2">Type</dt>
    <dd class="col-sm-10"><%= title.type %></dd>
  </dl>
<% end %>
<% if title.transliteration_standard.present? %>
  <dl class="row mb-0">
    <dt class="col-sm-2">Transliteration standard</dt>
    <dd class="col-sm-10"><%= title.transliteration_standard %></dd>
  </dl>
<% end %>
<% if title.language_code.present? %>
  <dl class="row mb-0">
    <dt class="col-sm-2">Language</dt>
    <dd class="col-sm-10"><%= language_name %></dd>
  </dl>
<% end %>
<% if title.script_code.present? %>
  <dl class="row mb-0">
    <dt class="col-sm-2">Script</dt>
    <dd class="col-sm-10"><%= script_name %></dd>
  </dl>
<% end %>
<% if title.value_at.present? %>
  <dl class="row mb-0">
    <dt class="col-sm-2">URL</dt>
    <dd class="col-sm-10"><%= title.value_at %></dd>
  </dl>
<% end %>
<% if title.display_label.present? %>
  <dl class="row mb-0">
    <dt class="col-sm-2">Display label</dt>
    <dd class="col-sm-10"><%= title.display_label %></dd>
  </dl>
<% end %>
<% if title.value.present? %>
  <dl class="row mb-0">
    <dt class="col-sm-2">Title</dt>
    <dd class="col-sm-10"><%= title.value %></dd>
  </dl>
<% end %>
<% if title.structured_values.present? %>
  <dl class="row mb-0">
    <% title.structured_values.each do |structured_value| %>
      <dt class="col-sm-2"><%= structured_value.type.titlecase %></dt>
      <dd class="col-sm-10"><%= structured_value.value %></dd>
    <% end %>
  </dl>
<% end %>
